cd ~/generative-llm-project
python3 -m venv venv
source venv/bin/activate

pip install torch
pip install torchvision torchaudio
pip install transformers datasets
pip install jupyter matplotlib
pip install tensorflow keras
